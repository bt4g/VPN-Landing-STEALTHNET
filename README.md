# VPN LANDING STEALTHNET

<img width="2046" height="1033" alt="image" src="https://github.com/user-attachments/assets/8158c2b6-348d-4492-9389-1807e9614054" />

**VPN Landing** — одностраничный сайт (SPA) на **React / Next.js**.

Проект ориентирован на продакшен-использование: SEO, генерация sitemap, админ-панель, несколько вариантов деплоя (Node.js, Docker, статика).

---

## 📋 Системные требования

* **Node.js** v20+
* **pnpm** (рекомендуется) или **npm**
* **Git**
* **Docker + Docker Compose** (опционально, для серверного деплоя)

---

## 💻 Локальная разработка

1. **Клонируем репозиторий:**
   ```bash
   git clone https://github.com/GOFONCK/VPN-Landing-STEALTHNET.git
   cd VPN-Landing-STEALTHNET
   ```

2. **Устанавливаем зависимости:**
   ```bash
   pnpm install
   ```

3. **Запускаем проект в режиме разработки:**
   ```bash
   pnpm dev
   ```
   > Сайт будет доступен по адресу: [http://localhost:3000](http://localhost:3000)

---

## 🚀 Деплой и запуск

Проект можно запускать и деплоить несколькими способами. Выбор зависит от того, **нужен ли SSR**, **есть ли Node.js** и **хочется ли Docker**.

---

### Вариант 1: Быстрая установка через Docker на свой сервер

Универсальный вариант для серверов и self‑hosted окружений.

> [!WARNING]
> **Требования:**
> - Открытые порты 80 и 443
> - DNS-запись домена должна указывать на IP сервера.

1. **Клонируем репозиторий на собственный сервер:**
   ```bash
   cd /opt
   git clone https://github.com/GOFONCK/VPN-Landing-STEALTHNET.git
   cd /VPN-Landing-STEALTHNET
   ```

2. **Запускаем скрипт `install.sh`**
   ```bash
   chmod +x install.sh
   ./install.sh
   ```

> [!IMPORTANT]
> **Скрипт запросит:**
> - Домен (например, `vpn.example.com`)
> - Пароль для админ-панели (назначаете свой пароль)
> - Email для Let's Encrypt (например, `admin@exmaple.com`)

---

### Вариант 2: Деплой на платформы с поддержкой SSR

Подходит для сервисов, где **Node.js и SSR уже есть из коробки**:

* Vercel
* Netlify
* Render
* Railway

> [!TIP]
> **Что происходит под капотом:**
> - Проект собирается командой `build`
> - Next.js запускается в SSR-режиме
> - API и админка работают полноценно

В тех или иных сервисах в конфигурациях проекта указываем готовый шаблон фреймворка (в нашем случае это **Next.js**), либо указываем вручную каждую из команд ниже:

1. **Установка зависимостей:**
   ```bash
   pnpm install
   ```

2. **Сборка проекта:**
   ```bash
   pnpm build
   ```

3. **Запуск сервера:**
   ```bash
   pnpm start
   ```

> [!NOTE]
> Обычно команду запуска платформа берёт из конфигурации автоматически. Если же нет, указываем её принудительно.

---

### Вариант 3: Статическая сборка (без Node.js)

Если нужен **чистый HTML / CSS / JS** без Node.js:

```bash
pnpm build:static
```

На выходе формируется папка `out/`, которую можно загрузить на любой статический хостинг:

* Обычный nginx / Shared-хостинг
* GitHub Pages
* Cloudflare Pages
* Netlify
* Vercel

> [!CAUTION]
> **Важно:** в статическом режиме админ-панель (`/admin`) работать не будет, так как для сохранения данных требуется работающее API на Node.js.
> 
> Запускайте проект локально и изменяйте данные через админ-панель или редактируйте данные напрямую в файлах `data/*.json` перед сборкой.

---

### Вариант 4: Настройка через Nginx на личном сервере

Если на сервере уже установлен Nginx или вы предпочитаете ручную настройку, следуйте инструкции: **[docs/INSTALL-NGINX.md](docs/INSTALL-NGINX.md)**.

---

## 🔐 Панель управления

Адрес: `vpn.example.com/admin`

- **Пароль по умолчанию:** `afina2025`
- **Смена пароля:** создайте файл `.env` в корне проекта и добавьте строку:
  ```env
  ADMIN_PASSWORD=your_password
  ```

**Возможности админки:**
- Управление тарифными планами (добавление, редактирование, удаление).
- Настройка контактов (Email, Telegram).
- Редактирование текстов оферты, соглашения и инструкций.
- Визуальная настройка дизайна (цвета, размеры шрифтов, скругления).
- Изменение порядка блоков на главной странице.

---

## 📂 Структура проекта

```
/
├─ app/              Страницы и роуты
├─ src/components/   UI-компоненты
├─ src/app/api/      Серверное API для работы админки
├─ data/             JSON-файлы с контентом сайта
├─ public/           Статика
├─ docs/             Документация
└─ out/              Результат static export
```